# URL de base de l'API backend (avec le préfixe /api).
# En dev, laissez vide pour utiliser la valeur par défaut http://localhost:3000/api
# En production, définissez l'URL réelle du backend déployé, ex:
# VITE_API_URL=https://api.finances.com/api
VITE_API_URL=
